version: 0.2
phases:
  install:
    commands:
      - npm i npm@9 -g
      - npm install
  build:
    commands:
      - npm run build
  post_build:
    commands:
      - aws s3 cp ./build s3://lab3-ellen --recursive
